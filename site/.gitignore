node_modules/
.next/
out/

# Generated at build time by Pagefind, mirrored here only for `next dev`.
public/pagefind/
.DS_Store
*.log

# .env.example is the committed template. Nothing the site reads is
# secret today — the site reads no variables at all — but the file these
# rules cover is the one that would hold it if that changed.
.env
.env.local
.env*.local
