/**
 * The package's JavaScript entry point.
 *
 * The tokens are not here — CSS is imported directly, as
 * `@english-learning/lib/tokens.css`, because a stylesheet has to reach
 * the document rather than a module graph.
 */
export * from "./icons";
