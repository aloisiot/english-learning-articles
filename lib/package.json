{
  "name": "@english-learning/lib",
  "version": "0.0.0",
  "private": true,
  "description": "Placeholder workspace for CSS tokens and React components shared between site/ and class/ (see research/video-calls). Populated starting Phase 5, once a second consumer exists.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  }
}
